include: cfg/model/tf.yaml

x_encoder: ResNetEncoder
resnet_layers: [2, 2, 2, 2]  # ResNet-18

# ContinualTransformer
tf_attn: elu  # vanilla or elu (Linear Transformer) or favor (Performer)
qk_dim: 512
v_dim: 512
